services:
    pokitoki:
        container_name: pokitoki
        restart: unless-stopped
        build:
            context: .
            dockerfile: Dockerfile
        extra_hosts:
            - "REPLACE_WITH_YOUR_DOMAIN.COM:host-gateway"
        volumes:
            - ./config.yml:/code/config.yml
            - data:/code/data

volumes:
    data:
